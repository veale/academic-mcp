[pytest]
asyncio_mode = auto
# Only collect the maintained suite. A couple of one-off scripts live at the
# repo root (test_search_logic.py, diagnose_semantic.py) and must not be
# treated as tests.
testpaths = tests
